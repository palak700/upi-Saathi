# UPI Saathi backend - Python 3.11+ (tested on 3.14)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12
pydantic>=2.8.0
pydantic-settings>=2.8.0
email-validator>=2.2.0
sqlalchemy>=2.0.40
psycopg[binary]>=3.2.0
alembic>=1.14.0
python-jose[cryptography]>=3.3.0
requests>=2.32.0
python-dotenv>=1.0.1

# RAG knowledge base
chromadb>=0.5.0
langchain>=0.3.0
langchain-community>=0.3.0

# QR code decoding and OCR
opencv-python-headless>=4.10.0
pyzbar>=0.1.9
pillow>=10.0.0

# Optional heavy extras (install separately when models are needed):
#   sentence-transformers>=3.0.0    # embeddings for ChromaDB
#   easyocr>=1.7.0                  # OCR fallback for QR decoding
#   faster-whisper>=1.0.0           # Whisper speech-to-text
#   gTTS>=2.5.0                     # text-to-speech audio generation
#   twilio>=9.0.0                   # SMS / WhatsApp confirmations